This render is an editorial pass over a whole document, not a summary. Output the entire document, top to bottom. Keep byte-identical and in order: the HTML img line under the title, every heading, every fenced code block, every inline code span, every link with its target, every number, every list item, and every table row (same count, same order, same cells apart from prose). Change only prose sentences, to tighten them and clear style-gate hits; add no fact, drop no fact, add no section, add no code span. The output is checked with respeak-verify-edit.py against the source and rejected on any structural difference.
