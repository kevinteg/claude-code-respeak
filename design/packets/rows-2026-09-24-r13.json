[
  {"id": "r13-fixes-adv11", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r13-fixes-adv11.md", "depends_on": ["packets-38-respeak"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 80, "why": "packet R13, off main, the ADV11 fixes test-first: act 1, under --committed a project file counts only inside the target's own work tree, so a nested repository's index no longer softens the check (ADV11-1, high); act 2, git_ask tells git's 'no' (exit 1) from a git failure (any other exit, a timeout, OSError), and a failure exits 2 instead of reading as untracked (ADV11-2, high); act 3, the renderer's account pattern takes 'limit reached' and 'rate_limit' and reads raw stdout (ADV11-5); act 4, survivor markers carry the run's pid and match whole, with decoys running (ADV11-4); act 5, the Makefile comment names the stop line anywhere in stderr (ADV11-3); no render; 80 turns for five small test-first acts"}
]
