[
  {"id": "r8-resync", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r8-resync.md", "depends_on": ["packets-30-respeak"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 60, "why": "the post-S10 re-sync in one builder: act 1 copies scripts/hygiene and scripts/doclint from the session's main 68c76cb and moves both Makefile pins in the same commit, with the email, lan and sibling allow rows the measurement needs and two doc cells written as config.local.yaml; act 2 makes every rows file's prompt repo-relative and splits the design/packets allow row so home stays only on the rows files (the repo field is the stand-in's contract) and on the r5 record, private kept on the sittings row (measured: five charters); act 3 copies scripts/bounded and runs check under it (lock .check.lock, wall 900 s, load 4 x cores) with a three-case suite; 60 turns because act 1 is one measurement-gated commit and act 2 touches eight files"}
]
