[
  {"id": "r9-readme-r7", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r9-readme-r7.md", "depends_on": ["packets-29-respeak"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 60, "why": "the README round for the respeak repo, one Opus row off main: design/readme/source.md rewritten as the finished public document (what, why the household relay program needs two lanes, install by the marketplace lines and make install with the doctor, the five skills and the agent with one example each, the scripts a reader runs, the layers table, the design of record and where decisions live, targeted tests and make check, the conventions shared with the sibling plugins by id only, MIT; References moved whole to docs/references.md under the 24,576-byte ceiling); a status line under Status that scripts/readme-render.sh writes before each render from plugin.json's version, the date, the unittest collection and the bash suite count, pinned by tests/test_readme.py and two test_render.sh cases; the megaphone icon moved to assets/icon.svg, the path the four plugins share, checked plain (no gradient, a title); the render through respeak's technical mode under the contract, at most two renders counted, the sidecar in the same commit; the public-repo scan (every hygiene group but person and sibling) reads 0 on the README and the source and no sibling path; 60 turns because the source is one rewrite and the render is last"}
]
