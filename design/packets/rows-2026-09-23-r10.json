[
  {"id": "r10a-fixes-adv10", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r10-fixes-adv10.md", "depends_on": ["packets-34-respeak"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 60, "why": "the two ADV10 highs, acts 1 and 2 of packet R10: gate --committed counts a project file only when git tracks it, reads it from the index, skips an untracked or ignored one as a named drop, and exits 3 outside git (ADV10-1); respeak-deadline.sh's watcher moves into its own process group as a one-second loop that also watches the script's pid, so a killpg at the supervisor's wall still stops the runner, with a 2 s inner grace under bounded's 5 s (ADV10-2, and ADV10-7 falls out); each repro is its act's first test; 60 turns for two test-first acts"},
  {"id": "r10b-fixes-adv10", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r10-fixes-adv10.md", "depends_on": ["r10a-fixes-adv10"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 70, "why": "acts 3 to 5 of packet R10, off main after r10a: the renderer fails on is_error or a non-success subtype with --out absent and exits 4 with one 'account failure' line for a usage-limit or login result, passed through readme-render.sh (ADV10-3, ruling 1); readme-render.sh renders against a temp copy of the source so a failed make readme leaves the tree clean, and the renderer keeps the source's final newline (ADV10-6, ruling 10); make readme once for the live count (ruling 11), stopping on an account failure; 70 turns because act 5 is a real render"}
]
