[
  {"id": "r7-fixes-r6", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r7-fixes-r6.md", "depends_on": ["packets-28-respeak"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 110, "why": "the R6 and ADV6 fixes in one builder: act 0 folds six charter-only decisions into CLAUDE.md, config-layers.md, the Makefile comment and the renderer header; act 1 makes the CI verdict hermetic (resolver --committed takes gate.* from the plugin defaults and the project file only, gate --file --committed exits 3 on any setup failure, respeak-check.sh unsets RESPEAK_CONFIG, CLAUDE_CODE_SESSION_ID and XDG_STATE_HOME, counts errors, checks the count invariant; ADV6-1, 2, T1); act 2 makes readme-fresh.sh run the verifier and refuse a stamp without its source (ADV6-3, R6-13); act 3 adds respeak-deadline.sh (process group, TERM then KILL, exit 124), a renderer with a depth guard, a load breaker, a wall clock, --tools and --max-turns, readme-render.sh that touches README.md only after the verifier passes, a 2 MB gate ceiling with measure under a deadline, validate and every suite under the helper (ADV6-5, 6, 7, 12; F2, F4, T6); act 4 resolves PYTHON without the pyenv shim and exports PATH, CLAUDE ?= claude for install (T2, F1; make doclint 20 s to 8 s measured); act 5 takes test_check.sh and test_report_env.sh off the real tree (F5, ADV6-11); 110 turns because the highs and the spawn class share three scripts"}
]
