[
  {"id": "r14-resync", "repo": "~/code/claude-code-respeak", "prompt": "design/packets/r14-resync.md", "depends_on": ["packets-41-respeak"], "worktree": true, "model": "claude-opus-5-5", "max_turns": 50, "why": "packet R14, off main; spawn ONLY after s15-resync has merged on the session's main (the orchestrator sets that dependency; act 0 also checks it and copies nothing if the merge is missing): act 0, bind the session's main once as P and check that each script's sha is the one its conventions section 6 names at P; act 1, one commit, the three canonical scripts byte-identical to P, HYGIENE_SHA and DOCLINT_SHA moved and a new BOUNDED_SHA pin, one CHANGELOG bullet; act 2 only if the new hygiene, doclint or bounded suite finds something; the shared account-cells table waits for ADV12-9 and is not copied; no README act; 50 turns for a copy-and-pin row"}
]
