# Sitting 2026-09-23-5: claude-code-respeak, adv-6-respeak

| item | value |
|---|---|
| goal | adversarial review of the respeak plugin as shipped: write `design/review-adv6.md` (ADV6-N findings, verdict, three tests); fix nothing |
| budget | 120 turns, 144k context; checkpoint at 80% |
| pin | `main` `2a21d76` |
| window | 200k, relay-run unattended |
| agents | none; the review reads code in-session |

## Done-ness

`design/review-adv6.md` (9,023 bytes): 12 findings, each reproduced in a temp directory or a `git archive` copy; nothing reviewed was edited. `make check` 0 once, before the review was tracked: the root's `.git/info/exclude` line `REVIEW-*.md` matches `review-adv6.md` on this case-insensitive disk, so the file was force-added, reworded to clear 14 hygiene hits (the plugin's name is written `RS` in it), then `scripts/hygiene` 0, `scripts/doclint` 0 and the gate `pass` were rerun on it alone.

## Handover

- Findings by severity: 4 high (ADV6-1, 2, 3, 9), 5 medium (ADV6-4 to 8), 3 low (ADV6-10 to 12).
- Verdict: not ready to carry syrvis. The CI gate can read green without a fix: an ignored folder sidecar (1), the environment or the session provider (2), a re-stamped README (3). Fix those and the renderer's missing wall clock, group kill and depth guard (5) first.
- The one fix that matters most: make the CI verdict (`respeak-check.sh`, `respeak-gate.sh --file`) hermetic, reading `fail_on` and `allow` from the committed project layer only and unsetting `RESPEAK_CONFIG`, `CLAUDE_CODE_SESSION_ID` and `XDG_STATE_HOME`. Consumer: the next respeak packet.
- For the owner (ADV6-9): the public repo carries relay rows with absolute paths under the owner's home and charters naming a private repo, admitted by `.hygiene-allow`. Decide whether relay rows and charters stay in this public repo; the path fix (repo-relative prompts) is safe either way. Consumer: the packet writer.
- ADV6-8's fix belongs in the canonical doclint (claude-code-session), not here. Consumer: the conventions packet.
- For the owner: the local exclude line `REVIEW-*.md` hides any `design/review-*.md` from hygiene, doclint and the check on this Mac; narrow it to the root. Consumer: the owner at the next Mac pass.
- For the orchestrator: merge `adv-6-respeak`; nothing pushed.

## Next act

None in this row. No `history/sessions/NEXT`: the fixes need a packet.

Exit: done, review written, 2026-09-23.
