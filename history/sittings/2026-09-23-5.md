# Sitting 2026-09-23-5: claude-code-respeak, review-6-respeak

| item | value |
|---|---|
| goal | the design-decisions review: one document `design/review-r6.md` (ledger, reversals, fork-storm class, timing, TLA+, the owner's list) and this charter; build nothing, fix nothing |
| budget | relay `--max-turns` per the CONTEXT line; land at 80%; 180k context |
| pin | `main` `2a21d76`; the defect brief macbase `history/relay/2026-09-23-defects.md` D1 to D7 |
| window | 200k, relay-run unattended |
| agents | none; the review is read and written in-session |

## Done-ness

`design/review-r6.md` at `360585d`, 9,122 bytes: 23 decisions (R6-1 to R6-23, six living only in a charter or packet, each with its home section), 7 reversals or tensions, 6 spawn sites (F1 to F6), 6 timing conditions (T1 to T6), the TLA+ line, 4 items for the owner. `make check` 0 in 55 s (218 unit tests, 8 bash suites, respeak-check 37 passed, 0 blocked, README fresh, two validates). Tree clean; no NEXT.

## Handover

- For the owner (the review's section 6): four items. Keep Apple's python as a hook-time candidate and record it in config-layers; rows files go repo-relative and the relay resolves against `repo`; CLAUDE.md says the gate is the pass for a few new lines; queue one packet R7 for the spawn and timing fixes. Consumers: the owner; the relay's row-shape owner; the R7 packet writer.
- Fork-storm class (section 3, read first): nothing here runs the relay, so no cycle closes through `relay plan`. Two spawn sites have no time bound: `make readme` runs `claude -p` with no `--max-turns` and no `timeout` (bounded only by its tool allowlist and two rounds, and `readme` is outside `check`); `make check` runs two real `claude plugin validate` at every measurement with no timeout. `tests/test_install.sh` runs a nested `make install` on the real tree, bounded only by a fake `claude` on PATH. `tests/test_check.sh` dogfoods the real checkout, which `make doclint` already does. Consumer: the R7 packet writer.
- Timing (section 4): the gate hook fails open on its 20 s timeout, so a loaded machine passes writes unmeasured; every top-level `make` resolves python through the pyenv shim (the D5 lock); no bash suite has a timeout. Consumer: the R7 packet writer.
- Choice: the review is tracked with `git add -f`. This checkout's local `.git/info/exclude` carries `REVIEW-*.md`, which matches `design/review-r6.md` on this case-insensitive disk; an untracked review is invisible to doclint and respeak-check until tracked. The sibling row `adv-6-respeak` (its worktree exists, untouched) meets the same rule. Consumers: the orchestrator; the owner, who may drop the exclude line.
- Choice: a whole-suite acceptance line here costs about a minute (measured 55 s), so the next packet names one bash suite and one unittest module instead of `make check` or `make test`. Consumer: the next packet writer.

## Next act

None in this row; no `history/sessions/NEXT`. The merge of the review is the orchestrator's; R7 is the owner's to queue.

Exit: done, the review and the charter committed, make check 0, 2026-09-23.
