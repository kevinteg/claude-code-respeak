# Sitting 2026-09-23-2: claude-code-respeak, r5-resync

| item | value |
|---|---|
| goal | packet R5: re-sync `scripts/hygiene` and `scripts/doclint` to the canonical copies |
| budget | 40 turns, 30k context; checkpoint at 80% |
| pin | branch `r5-resync` off `main` `1eac626`; copy source claude-code-session `27b22f9`, two blobs |
| window | 200k, relay-run unattended |
| agents | none; the builder works alone |

Exit: closed 2026-09-23, act 1 landed at `d917605`; `make check` 0 on the branch.

## Done-ness

Act 1 done (`d917605`): the two scripts are the pinned blobs byte for byte (shas
`5c7408dd`, `ca28e426`), the Makefile pins follow, the sittings allow row is
`sibling,private`, and `CHANGELOG.md` gains `## Unreleased` with one bullet. Act 2
not cut: `make doclint` exits 0 on the committed tree.

## Handover

- Finding: the new doclint checks a charter for the rows goal, budget, pin, window,
  agents. This charter first used the packet's row names and doclint went red; the
  rows were renamed before act 1 landed. Consumer: whoever writes the next packet,
  whose charter template should name those five rows.
- Choice made inside the design: the CHANGELOG bullet was written once in technical
  mode and the gate passed it. No separate editorial pass was run on four lines.
- Left for the owner: nothing to decide. The merge is the orchestrator's; no tag, no
  version bump, and no install until the final review pass.

## Next act

None. The row is done; `history/sessions/NEXT` is not written.
