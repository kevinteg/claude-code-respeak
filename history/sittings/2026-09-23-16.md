# Sitting 2026-09-23-16: claude-code-respeak, packets-35-respeak

| item | value |
|---|---|
| goal | write packet R11 (`design/packets/r11-resync.md`) and its rows file: the re-sync of the three canonical scripts after the session's S12 rows, and the `bounded` half of review ADV10; one commit, nothing built |
| budget | 60 turns, 144k context; checkpoint at 48 turns or 80% |
| pin | `main` `2095db5` (r10b merged) |
| window | 200k, relay-run unattended |
| agents | none; the coordinator works in-session |

## Done-ness

Done. Packet R11 and `design/packets/rows-2026-09-24-r11.json` (one row, `r11-resync`) are in one commit. Measured first, running the session's three scripts at `330bded` from a temp copy: hygiene finds 0 hits outside the allowlist. The new doclint refuses eight older packets for a missing `verify` row. `test_bounded.sh` against the new `bounded` reads 6 passed, 1 failed, because the flock lock file stays after a run. The new `bounded` closes all three ADV10 repros (the stop lines; 0 orphans after a group SIGKILL, where the old copy left 1).

## Handover

- Choice: the eight older packets (r1 to r7, r10) get a `verify` row that states the gate they ran under (`make check`), even though R8's wall calls a merged packet a record. GRANDFATHERED sits in the canonical doclint, which this repo never edits, and a new session row plus a new sha round costs more than one added row that states a fact. Consumer: the r11 builder; the session, if it ever widens GRANDFATHERED instead.
- Choice: act 1 carries everything `make check` needs to stay green (both pins, the 840 s wall, the verify rows, the one lock test the flock file changes). Act 2 carries the ADV10 tests, the `check` comment and the CHANGELOG bullet. Consumer: the r11 builder.
- Choice: the rows file keeps the `~` repo form, because this repo has no Go driver that reads a bare name (S12b's handover). Consumer: the relay.
- Not folded: `design/readme/source.md` still says "a 900 s wall". The next README round changes it to 840. The reader half of ADV10-4, where the gate treats the stop line as unrun, belongs to agent-relay's re-sync row. Consumer: the README round; the relay row.
- No open fork.

## Next act

The orchestrator drives `r11-resync` from the rows file; no `history/sessions/NEXT`.

Exit: done, packet and rows committed, make check 0, tree clean, 2026-09-23.
