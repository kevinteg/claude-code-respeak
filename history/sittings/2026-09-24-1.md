# Sitting 2026-09-24-1: claude-code-respeak, packets-37-respeak

| item | value |
|---|---|
| goal | write packet R12 (`design/packets/r12-resync.md`) and `design/packets/rows-2026-09-24-r12.json`: the post-S13 re-sync of hygiene, doclint and bounded, then a README act; one commit; I build nothing |
| budget | 60 turns, 144k context; checkpoint at 48 turns or 80% |
| pin | respeak `main` `e5f31ed`; the session's `main` `35b5870` (read, never run) |
| window | 200k, relay-run unattended |
| agents | none; the coordinator works in-session |

## Done-ness

Done. Packet R12 and its rows file are committed with this charter. Measured on `main` `e5f31ed` from a temp copy of the session's scripts at `35b5870`: hygiene 0 hits, doclint rc 0, `test_bounded.sh` 18 passed; TERM, INT, HUP in turn exit 129 on the old `bounded`, 143 on the new. `make check` ran once.

## Handover

- Choice: act 1 is copy and re-pin only. The measurement found no refusal, so there is no allow row and no packet edit. Consumer: the r12 builder.
- Choice: the S13b change (one cut per run, whatever signals follow) gets one bash case in act 2, red against the old copy. The session's pytest suites are not copied: this repo keeps its own shell suite for `bounded`. Consumer: the r12 builder.
- Choice: the README act changes only "900 s" to "840 s". The renderer writes the status line with the live test counts, so no count is typed by hand. Consumer: the r12 builder and the owner's merge review.
- Choice: the charter is `2026-09-24-1`, from the clock date. Consumer: the orchestrator.
- Nothing left undone for another row. No open fork.

## Next act

The orchestrator spawns `r12-resync` from `design/packets/rows-2026-09-24-r12.json`; no `history/sessions/NEXT`.

Exit: done, packet and rows file committed, make check 0, tree clean, 2026-09-24.
