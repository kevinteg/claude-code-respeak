# Sitting 2026-09-23-9: claude-code-respeak, r8-resync

| item | value |
|---|---|
| goal | build packet R8 (`design/packets/r8-resync.md`): the re-sync of hygiene and doclint, repo-relative relay rows, `check` under `scripts/bounded`; one commit per act |
| budget | 60 turns, 144k context; checkpoint at 80% |
| pin | `main` `c8d1478` (packet commit, after `5f571b3`); branch `r8-resync`; the session's `main` `68c76cb` read as three blobs |
| window | 200k, relay-run unattended |
| agents | none; the builder works in-session |

## Done-ness

Done. Act 1 committed (`e4c3ce4`): hygiene and doclint re-synced to `ca049635` and `3040666c`, pins moved, the allow rows and the two cells of §2 in place; `make hygiene` 0 outside the allowlist. Act 2 committed (`7b6a8c0`): five rows files' `prompt` made repo-relative (`repo` unchanged, a `~` path); the `design/packets` allow row split into `*.md`, `rows-*.json` and `r5-resync.md`. Act 3, with this charter: `scripts/bounded` copied (`e6079e68`), `check` runs `check-unlocked` under it, `.check.lock*` ignored, `tests/test_bounded.sh` 7 passed, one CHANGELOG bullet. `make check` 0 in 48 s (219 unittest cases, ten bash suites green, respeak-check 46 passed, 18 skipped); the lock is gone after the run.

## Handover

- Choice (act 1): the two cells write the file name as inline code, `config.local.yaml`, like the prose beside them in `config-layers.md`. Consumer: the builder.
- Choice (act 2): `private` stays on the sittings row. Measured: five lines under `history/sittings/` name private repos, in charters' pin rows; a charter is a record, never rewritten. Ruling 1 said the allow file drops the home and private groups; `home` left the packets and the sittings, `private` did not. Consumer: the owner, to overrule.
- Later (act 2): `home` on `design/packets/rows-*.json` retires when the relay resolves a bare repo name against the checkout beside it. Consumer: the agent-relay packet writer.
- Choice (act 3): the bounded suite runs the script with `python3` from `PATH` (the Makefile puts the virtualenv first), like the other suites run the hooks, and checks 7 cases: the three of §2 plus no survivor after the wall, the command not run under a held lock, and the lock released after a run. Consumer: the builder.
- Choice (act 3): `scripts/bounded` is unpinned (conventions §6 pins two scripts); `respeak-deadline.sh` stays for the suites, `validate` and the plugin's spawns. Consumer: the next re-sync packet writer, should conventions pin a third script.

## Next act

None in this row; no `history/sessions/NEXT`. The relay's verify line (`make check`) is the next step.

Exit: done, three acts committed, make check 0, tree clean, 2026-09-23.
