# Sitting 2026-09-22-4: claude-code-respeak, packets-10 (the R3 follow-ups packet)

| item | value |
|---|---|
| goal | one packet, `design/packets/r3-respeak-followups.md` (items 25, 26, 30, 38 of the decisions brief as ruled; the R1 and R2 handover follow-ups that are this repo's), baselines measured on main; one relay row in `design/packets/rows-2026-09-22-r4.json`; tree clean; no NEXT; exit line filled |
| budget | 180k context, 320 turns (the relay's ceilings); charter current at 80% |
| pin | main 6b1e529 at open, 2c432e5 after the packet act; claude-code-session main 4901b1b (conventions §1, §3, §5, §6; the canonical `scripts/hygiene` and `scripts/doclint`) |
| window | 200k |
| agents | 0; Fable coordinator writes the packet, no builder, no device, no push |

## Done-ness
- [x] charter written at open (e004633)
- [x] `design/packets/r3-respeak-followups.md`, 9,178 bytes, doclint 0, gate pass (2c432e5)
- [x] `design/packets/rows-2026-09-22-r4.json`, one row (2c432e5)
- [x] `make check` 0 on main before and after; tree clean; no NEXT

## Handover
For the owner, who rules; the packet only drafts. Choices made inside the design:
- Act 1 covers every reader of the old session variable, not the provider layer alone. Measured in this session: Claude Code's Bash tool sets `CLAUDE_CODE_SESSION_ID` and not `CLAUDE_SESSION_ID`, so `/respeak:off` refuses in a real session today. Eight files move to the one name. Consumers: the owner; the builder.
- Item 26's byte-identical gate is two sha256 pins in the Makefile checked by `make hygiene`, not a `cmp`: a public repo names no sibling path and the canonical repo is outside the wall. The re-sync is line 1 of each script (the measured diff). Consumers: the owner; the session's conventions owner, if the decisions repo wants the same shape.
- Item 30 needs no build here: the allowlist row from 17e488d is the one place, since the canonical script does not carry the rule. The session repo's own charters still cannot name a person (its row lacks `person`). Consumer: the session's next packet writer.
- The row's `prompt` is the absolute path this run asked for; it carries the home path, which the allowlist permits under `design/packets/*`. R2's row was repo-relative. Consumer: the relay's dispatcher.
- Not folded, named for R4: the `plugin/` subtree with `hooks/run`, `make install`, `make doctor`, `validate` on `plugin/`. Closed: the icon line under the README title; the `/`-rooted links. Items 36 and 39, the marketplace re-add and the session's dependency row stay yours or the session's. Consumer: the next packet writer.

The one open fork: the tag. No `v0.6.1` tag exists, so the packet grows the 0.6.1 CHANGELOG entry with no bump and no README render. Either tag `v0.6.1` after `r3-respeak-followups` merges (recommended), or tag it now at 6b1e529 and tell the builder to write 0.6.2 in both manifests, the CHANGELOG and the README's Status heading (one render round). Consumer: the owner, before the row runs.

## Next act
The relay runs row `r3-respeak-followups` (builder claude-opus-5-5, 60 turns) off `main`. No NEXT file: the next phase is the relay's.

Exit: landed at 2c432e5 plus this charter commit; tree clean; no NEXT; about 15 turns and 50k context of 180k.
