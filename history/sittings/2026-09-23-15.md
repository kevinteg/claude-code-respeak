# Sitting 2026-09-23-15: claude-code-respeak, r10b-fixes-adv10

| item | value |
|---|---|
| goal | build acts 3 to 5 of packet R10 (`design/packets/r10-fixes-adv10.md`): account failures in the renderer, the source and the final newline in `readme-render.sh`, and one `make readme`; one commit per act |
| budget | 70 turns, 144k context; checkpoint at 80% |
| pin | `main` `e661812` with r10a merged (`30888fa`); branch `r10b-fixes-adv10` |
| window | 200k, relay-run unattended |
| agents | none; the builder works in-session |

## Done-ness

Done. Act 3 (`646dddd`): the renderer fails on `is_error` true or a `subtype` other than `success`, writes `--out` only through a temp name, and exits 4 with `respeak-render: account failure: <first line>` when the result or a failed runner's stderr matches `ACCOUNT_FAILURE_RE`; `readme-render.sh` passes 4 through. Act 4 (`2d1ff1a`): `readme-render.sh` renders and verifies a copy of the source and moves it over only after the verifier passes; the renderer keeps the source's final newline. `test_render.sh` 51 passed. Act 5, with this charter: one `make readme`, exit 0 on the first attempt; `README.md` now ends in a newline. `make check` 0.

## Handover

- Choice (act 3): a runner that exits 0 with `is_error` true but a result that is not an account failure prints `respeak-render: the runner reported an error (is_error …, subtype …): <first line>` and exits 2, so the cause is visible. Consumer: the builder.
- Choice (act 3): `ACCOUNT_FAILURE_RE` matches `oauth token (has )?expired`, because the runner's message reads "OAuth token has expired"; the packet wrote "OAuth token expired". Consumer: the builder.
- Choice (act 3): the temp file sits beside `--out` and takes the caller's umask before the move, so `README.md` keeps its usual mode rather than `mkstemp`'s 0600. Consumer: the builder.
- Choice (act 4): the status-line refusal names `design/readme/source.md`, not the temp copy's path, so the existing test and a reader see the file they edit. Consumer: the builder.
- Act 5: the unittest count stayed at 227 because this row added only bash cases, so `design/readme/source.md` did not change and the commit holds `README.md` and the stamp. Consumer: the relay's verify line.

## Next act

None in this row; no `history/sessions/NEXT`. The relay's verify line (`make check`) is next.

Exit: done, three acts committed, make check 0, tree clean, 2026-09-23.
