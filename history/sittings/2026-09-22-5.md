# Sitting 2026-09-22-5: claude-code-respeak, r3-respeak-followups

| item | value |
|---|---|
| goal | Packet R3: the session id read from `CLAUDE_CODE_SESSION_ID` only; `scripts/hygiene` and `scripts/doclint` re-synced to the canonical sha256 and pinned in the Makefile; items 30 and 38 recorded |
| budget | 60 turns, 60k context; commit per act; no push, amend or rebase; no device |
| pin | `main` `52cf9c1` (descends from `6b1e529`); branch `r3-respeak-followups` |
| window | 200k; relay-run, unattended |
| agents | none; the builder works alone (claude-opus-5-5) |
| exit | clean: acts 1 and 2 committed, acts 3 to 6 recorded; `make check` 0; no NEXT (the next step is the owner's merge) |

## Done-ness

1. The session id comes from `CLAUDE_CODE_SESSION_ID` only, in all eight files. Done, committed with this charter update.
2. The two scripts re-synced (the shebang only; sha256 `fb05f620` and `f5a9ae84`) and pinned: `make hygiene` refuses a drifted copy before it scans. Done.
3. Charters and the `person` group (item 30): this repo's `.hygiene-allow` row `history/sittings/*.md: sibling,private,person` (on `main` since 17e488d) already covers them. Nothing was built. A canonical rule later removes the row in the same re-sync.
4. The hook's finder (item 38): recorded. `tests/test_gate_hook.sh` lines 164 to 170 stay as they are.
5. Follow-ups: act 1 closed R1's fifth bullet. The icon line under the README title and the `/`-rooted links stay. Named for R4: the `plugin/` subtree with `hooks/run`, `make install`, `make doctor`, and `validate` on `plugin/`.
6. No version bump: 0.6.1 is untagged, so its CHANGELOG entry grows and the manifests stay. `make readme` rendered nothing (no source change).

## Handover

- Choice inside the design: the one test that proves the old name is ignored builds that name at runtime (`"CLAUDE_CODE_SESSION_ID".replace("CODE_", "")`). A literal would break the acceptance grep, which must find no file outside the records. Consumer: whoever reviews the merge.
- Choice inside the design: the charter follows doclint's five-part shape, and no `history/sessions/NEXT` is written, because doclint refuses a NEXT file and the next step is the owner's merge.
- For the owner (items 36 and 39): read the CHANGELOG's two new 0.6.1 bullets at merge; tag `v0.6.1` after this branch merges. The Mac steps stay yours: the marketplace re-add as `respeak@claude-code-respeak`.
- For the session's row: if S7 changes `scripts/hygiene` or `scripts/doclint` again, re-sync the files and move the `HYGIENE_SHA`/`DOCLINT_SHA` pins in the Makefile in the same commit.
- Open fork: none.

## Next act

The owner merges `r3-respeak-followups` into `main`, then tags `v0.6.1`. R4 takes the `plugin/` subtree.
