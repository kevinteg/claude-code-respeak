# Sitting 2026-09-23-12: claude-code-respeak, adv-10-respeak

| item | value |
|---|---|
| goal | the second adversarial round: attack the code landed since the `adv-6-respeak` merge (`17030b4..353ce7a`), say whether each high and medium ADV6 fix holds, write `design/review-adv10.md` (ADV10-N ids, under 9,216 bytes); fix nothing |
| budget | 80 turns, 144k context; checkpoint at 80% (64 turns); `make check` at most once, at the end |
| pin | `main` `353ce7a` (after `r9-readme-r7`); branch `adv-10-respeak` |
| window | 200k, relay-run unattended |
| agents | none planned; the reviewer works in-session |

## Done-ness

Done. `design/review-adv10.md` written (8,441 bytes): eight findings, 2 high, 4 medium, 2 low, each with a repro run in a temp repo or a `git archive` copy; the nine ADV6 findings checked against the code as shipped (five hold, three hold in part, one is open as deferred). No reviewed file edited.

## Handover

- Count: 2 high (ADV10-1, ADV10-2), 4 medium (ADV10-3 to 6), 2 low (ADV10-7, 8). Consumer: the `packets-34-respeak` writer.
- Verdict: closer, not ready to carry the device project. The CI check can still be turned green by one untracked file (a nested project config), and the new time limits do not survive the kill a supervisor sends at its own limit. Consumer: the owner and the orchestrator.
- The one fix that matters most: ADV10-2, make every watchdog notice its parent's death and kill its child's group, so a killed sitting cannot leave a `claude -p` running with no clock. Consumer: `packets-34-respeak` and `packets-34-session` (the shared `scripts/bounded`).
- Routing: ADV10-4 (make check reports "not run" as a failure) and ADV10-5 (the lock race) live in the shared `bounded` and the conventions' `check` shape; they belong to the session plugin's packet and reach this repo by re-sync. ADV10-4 also touches how the relay reads its gate. Consumer: `packets-34-session`, `packets-34-relay`.
- Slip: the checkout's local exclude line `REVIEW-*.md` still matches `design/review-*.md` (git ignores case on this Mac), so the review was added with `git add -f`. Narrowing that line to the root (`/REVIEW-*.md`, run 6 ruling 2) is still undone. Consumer: the owner or the orchestrator, one edit outside this worktree.
- No open fork for the owner.

## Next act

None in this row; no `history/sessions/NEXT`. The relay's verify line (`make check`) is next.

Exit: done, one commit (review and charter), make check 0 (run once, at the end), 2026-09-23.
