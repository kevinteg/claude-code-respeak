# Sitting 2026-09-23-11: claude-code-respeak, r9-readme-r7

| item | value |
|---|---|
| goal | build packet R9 (`design/packets/r9-readme-r7.md`): the public README source, `docs/references.md`, the icon at `assets/icon.svg`, the status line the renderer writes, the render; one commit per act |
| budget | 60 turns, 144k context; `make readme` at most twice, counted; checkpoint at 80% |
| pin | `main` `674ce99` (the packet commit, after `2c8ec97`); branch `r9-readme-r7` |
| window | 200k, relay-run unattended |
| agents | none planned; the `respeak:respeak` agent only if the source needs an editorial pass |

## Done-ness

Done. Act 1: the public source (17,740 bytes, measure PASS, max sentence 29 words), `docs/references.md` (the seven reference groups byte-identical), `tests/test_readme.py` (8 cases; red until acts 2 to 4 land, and `readme-fresh` reads stale until act 4's render). Act 2: `git mv` to `assets/icon.svg`, the glyph shifted 1.5 left to center its stroked box (7.5 to 59.5 on x, 10.5 to 53.5 on y), 684 bytes, eyeballed once in headless Chrome. Act 3: the status step in `scripts/readme-render.sh`, five new checks in `tests/test_render.sh` (31 passed, 0 failed). Act 4: `make readme` once, passed in round 1 and verified; status line `0.6.1`, `227` cases, `10` suites; one CHANGELOG bullet; `make check` 0.

## Handover

- Choice (act 1): the old section holds seven groups, not six; all seven moved whole. Consumer: the builder.
- Choice (act 1): "Is an edit safe?", "Use" and "The knobs" fold into "Commands and skills" and "Where the tone comes from"; the verify-edit table and the prior-art paragraph go (the table's facts stay in `respeak-verify-edit.py`'s docstring). Consumer: the owner, to overrule.
- Choice (act 2): the megaphone stays, centered by one `translate` on the group; no new glyph. Consumer: the sibling packet writers, whose icons take distinct glyphs.
- Choice (act 3): the manifest is `plugin/.claude-plugin/plugin.json` under `--repo`; after the render, the first line under `## Status` is set back to the source's line, since the verifier treats the line's words as prose and a render could reword them. Consumer: the sibling packet writers who copy the step.
- Fork (for the owner): `tests/test_readme.py` checks the status counts against a live collection, so adding any test case turns `make check` red until `make readme` (one `claude -p` call) runs. That keeps the line honest; the other road is to check only the version and let the counts date with the render. Consumer: the owner.
- Slip: one early failing assertion printed the old README's text into context; the assertions now print short messages. Consumer: the orchestrator.

## Next act

None in this row; no `history/sessions/NEXT`. The relay's verify line (`make check`) is next.

Exit: done, four acts committed, `make readme` 1 of 2, make check 0, tree clean, 2026-09-23.
