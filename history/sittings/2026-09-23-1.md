# Sitting 2026-09-23-1: claude-code-respeak, packets-18

| row | value |
|---|---|
| goal | write `design/packets/r5-resync.md` and `design/packets/rows-2026-09-23.json`: the post-S8 re-sync of `scripts/hygiene` and `scripts/doclint` for one Opus builder, row `r5-resync`; build nothing |
| budget | 180k context, 320 turns; bring this charter current and exit at 80% |
| pin | `main` `bcaf292`, tagged `v0.6.1`; the copy source is the session repo's `main` `27b22f9` |
| window | 200k, relay-run unattended |
| agents | none |

## Done-ness

1. Reads: conventions §5, §6, §9; the S8 handover; packet R4 as the format; the R4 charter's handover.
2. Measured on `main`: both scripts' shas, the pins, the session's doclint and hygiene run on this tree, `make check`, the test counts, every acceptance line's baseline.
3. Packet written, under 9,216 bytes, green under the current doclint and the session's, and under the respeak gate.
4. Rows file written in the `rows-2026-09-22-r4b.json` shape.
5. One commit; tree clean; no `history/sessions/NEXT`.

## Choices made inside the design

- Act 2 is not cut: the session's doclint exits 0 on this tree (no `plugin/commands/`; `plugin/agents/respeak.md` carries the four keys). The packet has the builder re-measure with `make doclint` after the copy.
- The four moves land in one commit because the measurement says so: the old hygiene with the `person` allow narrowed is red on one charter; the new one is green either way.
- CHANGELOG: `v0.6.1` is tagged and the owner ruled no bump, so the packet adds an `## Unreleased` heading with one bullet. Whoever cuts the next version renames it.
- The copy source is the pinned blob (`git show 27b22f9:scripts/<name>`), not the session's working tree, so the byte-identity line does not depend on that tree's state.
- `make readme` is not in the packet: the README source, the docs and `CLAUDE.md` name no sha and no `pluginlint` (measured by `git grep`).

## Handover

- For the relay orchestrator: queue `design/packets/rows-2026-09-23.json` (row `r5-resync`, depends on `packets-18`, 40 turns), then merge `r5-resync` when its `make check` is 0.
- For the owner: nothing to decide. No tag, no install, no render (the 2026-09-23 ruling stands). The next version's cutter renames `## Unreleased`.
- Open fork: none.

## Next act

None in this row; the builder's row follows.

## Exit

Closed 2026-09-23: the packet and the rows file written and green under both doclints, the gate and `make check` 0; one commit; no `history/sessions/NEXT`.
