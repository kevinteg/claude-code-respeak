# Sitting 2026-09-22-1: packets-3, R1 for claude-code-respeak

Relay row `packets-3`. Coordinator: Fable. Branch `main`, HEAD at open `55d8826`.
Budget: 80 turns, 180k context. Commit per act, never push, never amend.

## Acts

1. Charter at open (this file).
2. Bounded reading: the session repo's plan row 8, conventions, overhaul §5 and D11,
   research §1 respeak row; the `s1-hygiene` packet as the format; `s4-meter` §2;
   this repo's manifest, marketplace, resolver headers, hygiene baseline; the relay
   row shape.
3. Write `design/packets/r1-respeak-conventions.md` (plan row 8). Commit.
4. Write `design/packets/rows-2026-09-22.json` (one relay row). Commit.
5. Verify: tree clean, no `history/sessions/NEXT`, charter exit line filled. Commit.

## Checkpoints

- open: charter written.

## Handover (for the owner; you rule, this drafts)

(filled at exit)

## Exit

(filled at exit)
