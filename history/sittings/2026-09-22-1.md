# Sitting 2026-09-22-1: claude-code-respeak, packets-3 (the R1 packet)

| item | value |
|---|---|
| goal | one packet, `design/packets/r1-respeak-conventions.md` (plan row 8), with its baseline measured on main; one relay row in `design/packets/rows-2026-09-22.json`; tree clean; no NEXT; exit line filled |
| budget | 80 turns, 180k context (the relay row packets-3); charter current at 80% |
| pin | main 55d8826 at open, b48d66d after the packet acts; claude-code-session main a78158d (the scripts the packet copies) |
| window | 200k |
| agents | 0; Fable coordinator writes the packet, no builder, no device, no push |

## Done-ness
- [x] charter written at open, then reshaped to the five-part form the session's doclint enforces (5a5819b)
- [x] bounded reading done: plan row 8 and baselines, conventions, overhaul §5 and D11, research §1, the two packets, this repo's manifests and resolver, the hygiene scan, the relay row shape
- [x] packet written and committed (e5d437c): 9,122 bytes, six sections, header table of six rows, twelve acceptance lines with baselines
- [x] relay row written and committed (b48d66d)
- [x] tree clean, no `history/sessions/NEXT`, exit line filled

## Handover
For the owner, who rules; the packet only drafts.
- The relay row's `prompt` is repo-relative, not absolute: this repo is public and the absolute form carries the home path. The row shape documents relative paths. If the relay wants absolute, edit that one field. Consumer: the relay's dispatcher.
- D11 applied: R1 renames the marketplace to `claude-code-respeak`. The session's dependency row then needs `"marketplace": "claude-code-respeak"`, and the marketplace registered on this Mac must be removed and re-added after merge. Consumers: the session's next packet writer; the owner.
- The python ruling and the canonical scripts collide: `scripts/hygiene` and `scripts/doclint` carry the Apple shebang and are copied byte-identical. The packet has the Makefile run them under the pyenv interpreter, so the shebang is dead here. Changing it, and the sha in conventions §6, is the session repo's act. Consumer: the session's conventions owner.
- The builder's bootstrap creates the pyenv virtualenv `claude-code-respeak` (3.12.7, PyYAML) on this Mac: drift for the deferred macbase freeze. Consumer: the freeze.
- R1 cut around, unruled: the plugin stays at the repo root (conventions §1 wants `plugin/`); no `readme`, `install` or `doctor` target; no version bump or CHANGELOG entry (0.6.1 is a release act); the four human-lane edits are code spans, one table row and one link, never prose, listed for the owner's editorial pass; the plugin's hook-time interpreter search keeps Apple's python as a candidate for other users' machines. Rule whether the python ruling reaches the plugin's runtime. Consumer: the owner.
- The public scan on main, counts only: home 1 line, domain 1, person 12 in 6 files, sibling 1114 in 67 (every one the plugin's own name), lan and private 0; 1128 outside a not-yet-existing allowlist. Research §1 said home 0 and sibling 1103/66; the scanner or the tree moved. The one domain hit is the author email the packet drops.
- Where the repo contradicts the row's sources: conventions §1 still lists respeak as the marketplace exception (void under D11); this repo's CLAUDE.md orders Apple's python for everything (the packet rewrites those lines).

## Next act
The relay runs row `r1-respeak-conventions` (builder claude-opus-5-5, 100 turns) after packets-3. No NEXT file: the next phase is the relay's, not a hand-run sitting's.

Exit: landed at b48d66d plus this charter commit; tree clean; no NEXT; about 22 turns and 80k context of 180k.
