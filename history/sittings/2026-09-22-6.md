# Sitting 2026-09-22-6: claude-code-respeak, packets-12 (the R4 packet)

| item | value |
|---|---|
| goal | Write `design/packets/r4-plugin-and-sync.md` and `design/packets/rows-2026-09-22-r4b.json`: the doclint re-sync (item 26), the `plugin/` subtree R3 named for R4 with its four parts decided inside conventions §1 to §5, the README sidecar (item 24) checked. Build nothing |
| budget | relay-run; 180k context and 320 turns, exit at 80%; one commit for the packet and the rows file; no push, amend, rebase or device |
| pin | `main` `cf4d924` (r3 merged); the builder's branch is `r4-plugin-and-sync` off `main` |
| window | 200k; unattended |
| agents | none; the coordinator read and wrote alone |
| exit | clean: the packet (9,156 bytes) and the rows file committed with this charter; `make check` 0; no NEXT (the next step is the relay's row) |

## Done-ness

1. Read: conventions §1 to §9, the R3 packet as the format, the R3 handover, the brief's items 24 and 26, the sha256 of both scripts here and in the canonical repo, this repo's tree by file name, the claude CLI's plugin subcommands (help and the read-only `--json` lists).
2. Measured on `main`: `make check` 0 in 44 s (218 tests); `scripts/hygiene` equals the canonical copy, `scripts/doclint` differs by the conventions ceiling only; no `plugin/` exists; the installed 0.6.1 cache holds all 100 tracked files.
3. Packet written under 9,216 bytes, six sections, six header rows, every acceptance line with a measured baseline. Done.
4. Rows file in the shape of `rows-2026-09-22-r4.json`: one row, `claude-opus-5-5`, 80 turns. Done.
5. `make check` green, tree clean, no NEXT, one commit. Done with this commit.

## Handover

- Choice: the `plugin/` subtree is built, not recorded away. The installed plugin today carries research, tests and history because the marketplace source is the repo root. Consumer: the builder; the owner at merge.
- Choice: `docs/config-layers.md` ships inside `plugin/docs/` because the skills and the agent name it as the contract; the human lane in CLAUDE.md grows `plugin/docs/`. "Self-contained" means no shipped file opens or executes a path outside `plugin/`; comments and prose may cite the repository's tree. Consumer: the builder.
- Choice: `hooks/run` is not built. It reaches a tool the plugin does not carry; respeak's hooks name their own scripts by the plugin root. If conventions §1 means the shim for every plugin, that is a conventions edit in the session repo, not a fork here. Consumer: whoever keeps the conventions.
- Choice: `make doctor` is a shipped script with six lines (python, claude, marketplace, plugin, provider, unknown keys). `make install` installs the plugin from this checkout and never removes a registration; it is act 4, built only if turns remain, else named for R5. Consumer: the builder; the owner, whose marketplace is GitHub-sourced today, so `make install` will stop and print the remove command for them to decide.
- Choice: the README sidecar keeps two lines. Line 1 is the ruled line and the check is `shasum -a 256 -c --status`; line 2 is CLAUDE.md's README tamper guard. If "one shape" means exactly one line, a later row drops it. Consumer: whoever keeps the conventions.
- Choice: the may-touch row names the eleven test files as `git ls-files tests` for the byte ceiling; every other entry is a full path. Consumer: the gate reader.
- Not folded: nothing from the prompt. The tag `v0.6.1` is still the owner's (R3's item 39); the packet tells the builder what to do if it lands first.
- Open fork: none.

## Next act

The relay runs `r4-plugin-and-sync` off `main` from `design/packets/rows-2026-09-22-r4b.json`. The owner merges, runs `make install` when they choose, and tags.
