# Sitting 2026-09-23-8: claude-code-respeak, packets-30-respeak

| item | value |
|---|---|
| goal | write packet R8 (`design/packets/r8-resync.md`) and its rows file (`design/packets/rows-2026-09-23-r6.json`): the post-S10 re-sync of the two canonical scripts, repo-relative relay rows, `check` under a bounded runner; build nothing |
| budget | this table is doclint's five rows; 40 turns, 144k context; one commit; `make check` at most twice; checkpoint at 80% |
| pin | `main` `5f571b3` (r7 merged); the session's `main` `68c76cb` read as blobs only |
| window | 200k, relay-run unattended |
| agents | none; the coordinator measures and writes in-session |

## Done-ness

Done. Measured on `main` `5f571b3` under the virtualenv (3.12.7): `make check` 0 (219 unittest cases, nine bash suites green, respeak-check 43 passed); the session's hygiene from this root reads 15 hits outside the allowlist in 11 files (lan 5, sibling 3, email 7), its doclint 0; every finding is an act in the packet. The packet, the rows file and this charter are one commit; the second `make check` proves it green.

## Handover

- Choice inside the design (act 2): the rows files' `repo` stays a `~` path, which the stand-in's docstring names as its contract (`~` allowed) and which hygiene reads as `home`. So `home` leaves the packets (`design/packets/*.md`) and stays on one row for `design/packets/rows-*.json`; `r5-resync.md`, a merged packet whose copy lines name the session checkout by a home path, gets a one-file `home` row rather than a rewrite of a record. Consumer: the builder; the agent-relay packet writer, since a bare repo name resolved by the relay against the checkout beside it would retire the rows row's `home`.
- Choice (act 2): `private` stays on the sittings row. Measured: five charters name the private repos in their pin rows (the defect brief's home). Ruling 1 said both groups drop; the charters are records. Consumer: the owner, to overrule.
- Choice (act 1): the two table cells that name the local config file by its short form (the new `.local` rule takes it for a hostname) are written as the file's full name, not a `lan` exception on public docs; the test fixture's `.local(` method gets the exception instead. Consumer: the builder.
- Choice (act 3): `scripts/bounded` is copied verbatim and left unpinned (conventions §6 pins two scripts); `respeak-deadline.sh` stays for the plugin's own spawns and for `validate`. Consumer: the next re-sync packet writer, should conventions pin a third script.
- Not folded: no `relay plan` line proves the rows file (the wall is this repo; the relay is elsewhere); the Go relay's reading of a relative `prompt` is the packets-30-relay row's. Consumer: the relay orchestrator.
- Open fork for the owner: none.
- Exit: the packet and the rows file committed with this charter, `make check` 0, tree clean, no `history/sessions/NEXT`, 2026-09-23.

## Next act

None here; the relay runs row `r8-resync` off `main`.
