# Sitting 2026-09-23-3: claude-code-respeak, packets-23

| item | value |
|---|---|
| goal | write packet R6 (`design/packets/r6-resync.md`) and its rows file for the post-S9 re-sync; build nothing |
| budget | 180k context, 320 turns; land the act in flight at 80% |
| pin | `main` `d9f4802` (tag `v0.6.1`); the session's canonical copies at its `main` `c7dbdee` |
| window | 200k, relay-run unattended |
| agents | none; the coordinator measured and wrote alone |

Exit: closed 2026-09-23; the packet, the rows file and this charter land in one commit; `make check` 0 twice on `main` (baseline, then with the three files).

## Done-ness

Measured before writing: the session's doclint, copied to a temp path and run from this root, exits 2 on exactly the five skill descriptions (297, 333, 206, 316, 274 chars) and nothing else; the session's hygiene is green with the sittings allow row narrowed, the old hygiene red on 47 lines if the row is narrowed first. Written: `design/packets/r6-resync.md` (9,194 bytes, the six sections, the six header rows), `design/packets/rows-2026-09-23-r2.json` (one row, `r6-resync`, `claude-opus-5-5`, 40 turns, depends on `packets-23`). Both doclints accept the packet and this charter.

## Handover

- Choice: act 2 is its own commit, not folded into act 1. Reason: act 1 stays a byte-for-byte copy plus pins, reviewable at a glance; the five description edits are content. Consumer: the builder of `r6-resync`.
- Choice: `.hygiene-allow` line 37 keeps `private` and loses only `sibling`; the row never carried `person`, so nothing else drops. Consumer: the builder.
- Choice: the copy source is named as the checkout beside this repo's main checkout, resolved from `git rev-parse --git-common-dir`, never a home path; the packet names no sibling beyond the session repo. Consumer: the builder; the hygiene `home` group.
- Choice: per skill the packet lists the words that survive. Four skills are user-invoked only, so their description is menu text; `respeak` is model-invoked and keeps at least three of its five quoted asks, since all five cannot fit under 200 with the invocation line; `plugin/agents/respeak.md` keeps the full list. Consumer: the builder; whoever tunes the skill's firing later.
- Choice: one CHANGELOG bullet under the existing `## Unreleased`, written once in technical mode as R5 did. Consumer: whoever cuts the next version.
- Could not fold: the two old shas stay in the R5 packet and two charters (records). The new doclint's skill rule reads `>` blocks only; the five skills already use that shape. Nothing goes to another repo.
- Open fork for the owner: none.

## Next act

None here. The orchestrator runs the `r6-resync` row from the rows file, then merges; `history/sessions/NEXT` is not written.
