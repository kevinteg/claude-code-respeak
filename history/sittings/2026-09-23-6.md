# Sitting 2026-09-23-6: claude-code-respeak, packets-28-respeak

| item | value |
|---|---|
| goal | one fix packet from the two reviews (`design/review-r6.md`, `design/review-adv6.md`): `design/packets/r7-fixes-r6.md` and its rows file `design/packets/rows-2026-09-23-r5.json`; build nothing |
| budget | relay `--max-turns` per the CONTEXT line; land at 80%; 180k context |
| pin | `main` `17030b4`; the defect brief macbase `history/relay/2026-09-23-defects.md` D2 |
| window | 200k, relay-run unattended |
| agents | none; the packet is read and written in-session |

## Done-ness

The packet (six acts, under 9,216 bytes) and the rows file (one row, `r7-fixes-r6`, claude-opus-5-5, 110 turns, `depends_on` `packets-28-respeak`), committed with this charter. Carried: every ADV6 high but ADV6-9 (ADV6-1, 2, 3 in acts 1 and 2), every unbounded spawn and timing condition of R6 (F1, F2, F4, F5, T1, T2, T6 in acts 3 to 5), the mediums ADV6-5, 6, 7 and the lows 11 and 12, and the fold of R6-4, 6, 11, 12, 13, 19 into the design of record (act 0). Deferred, named in the packet: ADV6-4 (its own packet; it changes every repo's verify), ADV6-8 (the canonical doclint in claude-code-session), ADV6-10 (a cached `sys.executable` breaks the interpreter cache's trust boundary; act 4 removes the shim instead), R6-18 (the README packet). `make check` 0 once at the end; tree clean; no NEXT.

## Handover

- The one fork, for the owner (ADV6-9 with R6-22): the public repo tracks relay rows carrying the owner's home path and charters naming a private repo, admitted by `.hygiene-allow`. Keep them here (then rows go repo-relative once the relay resolves `prompt` against `repo`, and the allow file drops `home` and `private`) or move them out. Today's rows file keeps the absolute shape, the one that ran for R3 to R6. Consumers: the owner; the relay's row-shape owner; the next packet writer here.
- Choice, inside the design (act 1): the CI verdict reads `gate.*` from the plugin defaults and the project file with its scopes only, as an opt-in `--committed` mode; the hook keeps the documented open folder rule. A folder that wants leniency in CI says so in the project file's `scopes:`, one reviewable file. Consumer: the builder; config-layers' CI paragraph records it.
- Choice (act 3): one bounded-execution primitive, `respeak-deadline.sh` (own process group, TERM then KILL, exit 124), serves the renderer, the gate's measure, `validate` and the test loop, because a stock Mac has no `timeout`. The renderer gains the D2 load breaker (refuse to spawn above 4x cores) and a depth guard. Consumer: the builder.
- Choice (act 4): the Makefile resolves the virtualenv from `.python-version` with no process and exports its bin dir first on PATH; measured `make doclint` 20 s to 8 s. R6-4 is decided yes: Apple's python stays a hook-time candidate for other machines. Consumer: the builder.
- R6 items 1 to 3 were not ruled on the CONTEXT line, so they are decided inside the design as the review recommended and folded in act 0; item 4 is this packet. Consumer: the owner, to overrule.
- For the orchestrator: the row is unblocked when this row lands; `make check` on the branch is the verify line; every acceptance line is targeted and under a minute.

## Next act

None in this row; no `history/sessions/NEXT`. The build is the row `r7-fixes-r6`.

Exit: done, the packet, the rows file and this charter committed, make check 0, 2026-09-23.
