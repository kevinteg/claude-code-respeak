# Sitting 2026-09-24-5: claude-code-respeak, r13-fixes-adv11

| row | value |
|---|---|
| goal | packet R13: the ADV11 fixes (ADV11-1, 2 high; 5, 4, 3 medium), five acts |
| budget | 80 turns, 144k context; one commit per act; checkpoint at 80% |
| pin | `main` at `9a87b60` (packet R13 on `5daf506`) |
| window | 200k; relay-run, unattended |
| agents | none planned; the builder works inline |

## Done-ness

`make check` 0 at every act; the §6 lines at or past their targets; the charter under 4,096 bytes with no home path.

## Handover

Acts 1 to 5 landed, `make check` 0 after each: act 1 `46a6c7e`, act 2 `62a082c`, act 3 `3aad054`, act 4 `d7afd22`, act 5 `c68a7f4`. Tests: check 45 to 59, render 51 to 58, bounded 19 to 20.

Choices beyond the packet, each inside its design:
- Act 1: "the target's toplevel" is the work tree whose index tracks the target, found from the innermost repository outward (`target_work_tree`). The toplevel of the target's directory alone is the nested repository in ADV11's repro, and it would have let that file govern. A project reached by the fallback (nearest `.git`) is read through the same check.
- Act 2: `respeak-gate.sh` is outside the may-touch list and discards the resolver's stderr, so on a gate exit 3 `respeak-check.sh` asks the resolver once more and stops with its `git failed` line.
- Act 4: the deadline's timer is a `sleep 1` no test can tell apart, so the marker is the watchdog's whole command line (`bash <deadline> 43<pid> true`), which owns that timer. Both suites also check that the decoys are still alive at the end.

Undone, for other rows: the relay's shared account pattern and its gate reading (ADV11-3, 5); ADV11-6 in the canonical doclint; the canonical `bounded` suite takes act 4's shape. No decision is waiting on the owner.

## Next act

None in this row. The orchestrator merges `r13-fixes-adv11`. No `NEXT` is written.

Exit: closed 2026-09-24, five acts, `make check` 0, clean tree.
