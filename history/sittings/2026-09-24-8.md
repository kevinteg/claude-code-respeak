# Sitting 2026-09-24-8: claude-code-respeak, r15-resync

| item | value |
|---|---|
| goal | packet R15: scripts/readme-render.sh runs the renderer as a marked helper (relay-r1 §10), refused past depth 2 |
| budget | one row, 50 turns, 144k context (80% of 180k); one commit per act; make check in the foreground before each commit |
| pin | respeak main 3c1f63d; agent-relay main b8d789b (last commit on design/relay-r1.md); S = §10's helper sentence, sha256 3435f321ecd1 |
| window | 200k |
| agents | 0 |

## Done-ness
- [x] act 0: packet copied in verbatim (cp, cmp)
- [x] act 1: charter opened; S cut from relay-r1 §10 at b8d789b (one line, 295 bytes), not empty
- [x] act 2: the render line runs as `CLAUDE_SESSION_DEPTH=$((d + 1)) CLAUDE_SESSION_HELPER=1`; RELAY_ROW and CLAUDE_SESSION_MAX_TURNS pass through; a depth that is not a plain non-negative integer, or d + 1 over 2, exits 2 in setup before the temp dir; the comment quotes S on one line; one CHANGELOG bullet; readme-fresh.sh does not name the helper, so it is unchanged
- [x] tests: d = 1 (the fake claude sees depth 2, helper 1, the row and the turns as given), d = 2 and d = x1 (exit 2, no runner, nothing written); 68 passed, 0 failed; against main 8 fail (all new cases red)
- [x] act 3: skipped, act 2 moved no count (227 unittest cases, 10 bash suites before and after); no `make readme`, README.md and the stamp untouched
- [x] act 4: this close; make check 0 at every commit

## Handover
- Choice inside §2: the refusal message names the depth and says "refused past depth 2"; a depth with leading zeros counts as its integer; a depth of two or more digits after them is past depth 2, so no overflow reaches the arithmetic.
- Choice inside §2: the new cases set HOME and the XDG homes to temp dirs; the pyenv shim then picked a python without PyYAML, so the cases pass PYTHON and RESPEAK_PYTHON resolved under the caller's HOME first. The suite unsets the four helper variables at its top, so a caller's depth never refuses the older readme-render cases.
- The fork: none; the render never ran.
- exit taken: r15-resync done; no NEXT.

## Next act
None in this row: the owner merges r15-resync; no NEXT.
