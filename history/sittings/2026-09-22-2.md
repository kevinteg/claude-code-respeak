# Sitting 2026-09-22-2: claude-code-respeak, R1 conventions (relay row r1-respeak-conventions)

| item | value |
|---|---|
| goal | the packet `design/packets/r1-respeak-conventions.md`: Makefile, `.python-version`, hygiene and doclint copies, `.hygiene-allow`, manifests, the session provider layer, the human-lane spans; the twelve acceptance lines green; tree clean |
| budget | 100 turns, 120k context (the packet); checkpoint at 80% |
| pin | branch `r1-respeak-conventions` at c45b5cd at open, e4625dc after the acts; claude-code-session main a78158d |
| window | 200k |
| agents | 0; relay-run builder, unattended, no push, no device |

## Done-ness
- [x] charter written at open (9379352)
- [x] copied scripts, Makefile, `.python-version`, virtualenv (fe67e8d)
- [x] `.hygiene-allow` and the placeholder rewrites (c01254d); hygiene exits 0
- [x] manifests: marketplace renamed, author email dropped (3512012)
- [x] provider layer in the resolver, the gate passes `--session`, tests (7710332)
- [x] human-lane spans: CLAUDE.md, README.md, docs/config-layers.md, the research link (e4625dc)
- [x] twelve acceptance lines green; tree clean; no NEXT

## Handover
For the owner, who rules.
- Owner's editorial pass over the human-lane edits: CLAUDE.md (one command line; the "Before committing" sentence now names `make check` and the virtualenv); README.md lines 372 and 441 (`respeak@claude-code-respeak`; the packet said three spans, the README had two); docs/config-layers.md (the new row 8 `session`, env and invocation renumbered 9 and 10); the research page's `[text](url)` example escaped as `[text]\(url)`. Consumer: the owner.
- The bash suites find `python3` on PATH, which outside the repo is pyenv's `system`. The Makefile puts the virtualenv's directory first on PATH for them; `test_overrides.sh` and `test_gate_hook.sh` (outside the wall) still say bare `python3` and pass only that way. Consumer: the next respeak packet.
- Marketplace rename: the session's dependency row needs `"marketplace": "claude-code-respeak"`; on this Mac, after merge, remove the `respeak` marketplace, add this repo, reinstall. Consumers: the session's next packet writer; the owner.
- The virtualenv `claude-code-respeak` (pyenv 3.12.7, PyYAML 6.0.3) now exists on this Mac: drift for the deferred macbase freeze. Consumer: the freeze.
- The session layer reads `$CLAUDE_SESSION_ID` first (as the packet says), then `$CLAUDE_CODE_SESSION_ID`; conventions §3 names only the second. The provider line shows in `explain` and `explain --brief`. Consumer: the session's conventions owner.
- Left undone, as ruled: no version bump or CHANGELOG entry, no `plugin/` subtree, no readme/install/doctor targets.

## Next act
The relay's gate reruns the twelve lines on this branch; the owner merges. No NEXT file.

Exit: landed at e4625dc plus this charter commit; tree clean; no NEXT; about 60 turns and 110k context of 180k.
