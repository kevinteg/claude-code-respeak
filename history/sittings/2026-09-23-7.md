# Sitting 2026-09-23-7: claude-code-respeak, r7-fixes-r6

| item | value |
|---|---|
| goal | build packet R7 (`design/packets/r7-fixes-r6.md`): the fold, a hermetic check, the stamp, bounded spawns, the interpreter and the install, tests off the real tree; one commit per act |
| budget | 110 turns, 144k context; checkpoint at 80% |
| pin | `main` `5d6da09` (packet commit, after `17030b4`); branch `r7-fixes-r6` |
| window | 200k, relay-run unattended |
| agents | none; the builder works in-session |

## Done-ness

Done. Acts 0 to 5 committed (`24f6122`, `b3e72af`, `9e0f511`, `9e3d8ba`, `a36bb31`, then act 5 with this charter). `make check` 0 in 48 s (main: 55 s). Section 6 on the branch: test_check 40 passed, test_readme_fresh 11, test_render 26, all 0 failed; dogfood 0; renderer grep 10; Makefile grep 2 and 1; tree clean.

## Handover

- Choice inside the design (act 1): `--committed` also drops `gate.*` from `config.local.yaml`. The packet says "the project file"; the local file is gitignored, which is the ADV6-1 vector. Consumer: the owner, to overrule; config-layers' CI paragraph says "committed project file".
- Choice (act 4): the Makefile names the virtualenv (`VENV = claude-code-respeak`) because make 3.81 cannot read `.python-version` without a process; `make lint` exits 2 when the two differ. Consumer: whoever renames the virtualenv.
- Choice (act 3): `readme-render.sh` renders into `.readme-render.XXXXXX` inside the repository, so the render resolves the same layered configuration as `README.md`. A render killed with KILL can leave that directory untracked. Consumer: the next packet writer here.
- Choice (act 2): the stamp test edits the README with a code span and a number. The verifier still admits a new prose paragraph, which is ADV6-4, deferred by the packet. Consumer: the ADV6-4 packet.
- Not done, outside the rulings: T1's "gate: skipped" line on the hook's timeout path; F4's "a skipped validate never reads green"; ADV6-2's "a skip that is not an exclude is a failure". Deferred by the packet: ADV6-4, ADV6-8, ADV6-10, R6-18. Consumer: the relay orchestrator, for the next fix packet.
- Seen, not changed: `tests/test_hooks.sh` fails six statusline cases when run by hand with the pyenv shim first on `PATH`; green under make, whose `PATH` export (act 4) now covers every recipe. Consumer: the next packet writer here.
- The one fork stays the packet's: ADV6-9 with R6-22 (relay rows and charters in a public repo). Consumer: the owner.

## Next act

None in this row; no `history/sessions/NEXT`. The relay's verify line (`make check`) is the next step.

Exit: done, six acts committed, make check 0, tree clean, 2026-09-23.
