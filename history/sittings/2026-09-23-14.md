# Sitting 2026-09-23-14: claude-code-respeak, r10a-fixes-adv10

| item | value |
|---|---|
| goal | build acts 1 and 2 of packet R10 (`design/packets/r10-fixes-adv10.md`): a committed-only project file under `gate --committed`, and watchdogs outside the caller's kill; one commit per act |
| budget | 60 turns, 144k context; checkpoint at 80% |
| pin | `main` `e661812`, packet commit `418630e`; branch `r10a-fixes-adv10` |
| window | 200k, relay-run unattended |
| agents | none; the builder works in-session |

## Done-ness

Done. Act 1 committed (`581f22f`): `gate --committed` reads the nearest tracked project file from the index, skips an untracked or ignored one and names it in `dropped`, and exits 2 outside a git work tree. `test_check.sh` 45 passed; `tests.test_config_layers` OK. Act 2, with this charter: `respeak-deadline.sh` runs its watchdog in its own process group on a one-second tick, stops the command's group when the script is gone, grace 2 s, and kills the watchdog's group when the command ends first. `test_render.sh` 34 passed (twice, 10 s each); `test_bounded.sh` 7 passed; `make check` 0.

## Handover

- Choice (act 1): the outside-git case is a first step inside the moved `--committed` test, run before its `git init`, not a new test method. A new method moves the live unittest count (227 to 228) that `test_readme.py` pins, and only act 5's render updates the count, so this row's `make check` would read red. Consumer: the r10b builder, whose act 5 render counts whatever cases exist then.
- Choice (act 1): the `dropped` path is relative to the target's git top level (`docs/.claude/respeak/config.yaml`); an explicit `--project` is kept verbatim and its file is read from the index, absent when untracked. Consumer: the builder.
- Choice (act 2): the killpg tests mark their processes by a distinct sleep length (`sleep 45.25`, and the fake runner's `FAKE_SLEEP=44.25`), the way the suite's earlier cases do, not by an extra argument. The fake runner takes `FAKE_SLEEP`; the exit trap sweeps `sleep 4[4-7].25` and `sleep 43`. Consumer: the r10b builder, who adds to the same suite.
- Choice (act 2): when the script is gone the watchdog stops the group without writing its mark, so the 124 line is only printed by a live script. Consumer: the builder.

## Next act

None in this row; no `history/sessions/NEXT`. The relay's verify line (`make check`) is next, then row `r10b-fixes-adv10` (acts 3 to 5).

Exit: done, two acts committed, make check 0, tree clean, 2026-09-23.
