# Sitting 2026-09-24-4: claude-code-respeak, packets-38-respeak

| row | value |
|---|---|
| goal | packet R13 from `design/review-adv11.md`: ADV11-1 and ADV11-2 first, then the mediums; one rows file naming one Opus builder; build nothing |
| budget | one relay row, packet writer; 144k context; one commit per act; checkpoint at 80% |
| pin | `main` at `5daf506` |
| window | 200k, unattended relay run |
| agents | none; the packet is written inline |

Doclint's five charter rows, named at open: goal, budget, pin, window, agents.

## Done-ness

- act 0: this charter.
- act 1: `design/packets/r13-fixes-adv11.md` (five acts: ADV11-1, 2, 5, 4, 3) and `design/packets/rows-2026-09-24-r13.json` (one row, `r13-fixes-adv11`, `claude-opus-5-5`, 80 turns, worktree off main). Baselines measured on `5daf506`: `test_check.sh` 45, `test_render.sh` 51, `test_bounded.sh` 19, `tests.test_config_layers` OK.

Exit: closed 2026-09-24 after act 1; the packet and rows file in one commit; `make check` result in the commit message; nothing pushed; no NEXT.

## Handover

Carried: ADV11-1 and 2 (highs, acts 1 and 2), ADV11-5, 4 and 3 (mediums, acts 3 to 5). Deferred: ADV11-6 (low) to the canonical doclint, reaching this repo by re-sync; consumer: the session plugin's packet.

Choices inside the design, recorded here: a git failure under `--committed` exits 2 (setup), never 0, and `respeak-check` stops at the first one, since it spoils every file; an ancestor project file outside the target's work tree is dropped without a git call, and one in a nested repository is dropped after a strict `rev-parse`; the relay owns the canonical account pattern (ruling 1), and this renderer keeps a tested copy until a re-sync; act 4's per-run markers also go to the canonical `bounded` suite; respeak's half of ADV11-3 is a comment, and the gate's reading is the relay's (consumer: the relay's packet). No new unittest case or test file, so no README render and no account risk in the row.

Open fork: none.

## Next act

None in this row; the builder row `r13-fixes-adv11` builds the packet.
