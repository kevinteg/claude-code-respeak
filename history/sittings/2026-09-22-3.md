# Sitting 2026-09-22-3: claude-code-respeak, R2 the README through respeak (relay row r2-readme)

| item | value |
|---|---|
| goal | the packet R2: `design/readme/source.md`, `make readme` through `scripts/respeak-render.sh`, `scripts/readme-fresh.sh` and its suite, version 0.6.1, CHANGELOG entry, CLAUDE.md paragraph, the two suites' interpreter; the fifteen report lines green; tree clean |
| budget | 60 turns, 60k context (the packet); checkpoint at 80% |
| pin | branch `r2-readme` off `r1-respeak-conventions` at bfdc38a |
| window | 200k |
| agents | 0 planned; relay-run builder, unattended, no push, no device |

## Done-ness
- [x] charter written at open (0c7438c)
- [x] suites' interpreter `"${PYTHON:-python3}"` (8b680e6)
- [x] `scripts/readme-fresh.sh`, its suite, Makefile targets (77edd6e)
- [x] `design/readme/source.md`, config scope, `.hygiene-allow` (c3af266)
- [x] `make readme` rendered, verify-edit 0, stamp (4c49ffb)
- [x] version 0.6.1, CHANGELOG, CLAUDE.md (f03e432)
- [x] fourteen report lines green; tree clean; no NEXT

## Handover
For the owner, who rules.
- Please do an editorial pass over two pieces I wrote once: the CHANGELOG `0.6.1` entry and the CLAUDE.md paragraph "The README is rendered" (plus `design/readme/source.md` in its human-lane sentence). Consumer: the owner.
- Two changes to `make readme` that the packet did not name. First, it passes the renderer a `--contract` (the text is in the Makefile's `README_CONTRACT`): this is an editorial pass, keep the structure. Without it, round 1 summarized the whole document and verify-edit failed on everything. Second, the icon `<img>` line now sits under `# respeak`, because `respeak-render.sh` strips everything above the first heading as preamble. A later packet could fix that in the renderer instead. Consumer: the owner; the next respeak packet.
- The README's links now start with `/` (for example `/docs/config-layers.md`). That form resolves from the repo root on GitHub and in `scripts/doclint`, so one target works from both the source in `design/readme/` and the README at the root. Plain relative links would fail doclint in the source. Consumer: the owner.
- After merge, on this Mac: `claude plugin marketplace remove respeak`, then `claude plugin marketplace add kevinteg/claude-code-respeak` and `claude plugin install respeak@claude-code-respeak`. The session's dependency row needs `"marketplace": "claude-code-respeak"`. Consumers: the owner; the session's next packet writer.
- Left undone, as ruled: the tag and the push (the owner's release act), the `plugin/` subtree, the `install` and `doctor` targets.

## Next act
The relay's gate reruns the report lines on this branch; the owner merges. No NEXT file.

Exit: landed at f03e432 plus this charter commit; tree clean; no NEXT; 36 turns, about 55k context of the 60k packet budget.
