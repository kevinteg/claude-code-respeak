# Sitting 2026-09-22-3: claude-code-respeak, packets-7 (the R2 README packet)

| item | value |
|---|---|
| goal | one packet, `design/packets/r2-readme.md` (plan row 14, R2: the README through respeak, `make readme`, the freshness line, the R1 follow-ups that are this repo's), baselines measured on main and on the base branch; one relay row in `design/packets/rows-2026-09-22-readme.json`; tree clean; no NEXT; exit line filled |
| budget | 180k context, 320 turns (the relay's ceilings); charter current at 80% |
| pin | main c45b5cd at open, 3305db9 after the packet acts; base branch r1-respeak-conventions at bfdc38a, unmerged (the owner merges); claude-code-session main a78158d (conventions §5, §6) |
| window | 200k |
| agents | 0; Fable coordinator writes the packet, no builder, no device, no push |

## Done-ness
- [x] charter written at open (9f62862)
- [x] bounded reading done: the R1 packet as the format, the R1 handover, README by section, docs/config-layers.md by section, conventions §5 and §6, overhaul §5, the relay row shape, the renderer's usage, the base branch's `make check` (0)
- [x] packet written and committed (94b43a3): 9,163 bytes, six sections, fifteen acceptance lines with main and base baselines
- [x] relay row written and committed (3305db9)
- [x] tree clean, no `history/sessions/NEXT`, exit line filled

## Handover
For the owner, who rules; the packet only drafts.
- The row's `prompt` is repo-relative, as R1's was: the repo is public and an absolute path carries the home path; the relay's row shape accepts either. Consumer: the relay's dispatcher.
- Freshness is a content stamp, `design/readme/rendered.sha256` checked by `shasum -c`, not the mtime conventions §5 words as "older than": git keeps no mtimes across clones. The session's own `readme` line may want the same shape. Consumer: the session's conventions owner.
- The render is proven prose-only, `respeak-verify-edit.py` source against README, so the README's headings, links, code and numbers are the source's. The source ceiling is 24,576 bytes, four times the session's 6 KB, because the research case and References stay. For the session's ceiling, edit the packet's §2 and the third §6 line before the relay runs. Consumer: the owner.
- Version 0.6.1 with a CHANGELOG entry is in the packet on this prompt's word, over packets-1's "the owner's release act"; the tag and the push stay the owner's. Consumer: the owner.
- D11's other halves stay outside the wall: the session's dependency row `"marketplace": "claude-code-respeak"` (packets-5) and the re-add on this Mac, which the README's new Install paragraph spells out. Consumers: packets-5's builder; the owner.
- The python ruling reaches the two bash suites as `"${PYTHON:-python3}"`; the hook's own interpreter finder and its broken-shim fixture stay, still unruled (R1's question). Consumer: the owner.
- Not folded: the editorial pass over the new CHANGELOG entry and CLAUDE.md paragraph (listed for the owner's pass, as R1's); the `plugin/` subtree, `install`, `doctor` (later rows); the macbase freeze drift. On main, doclint refuses one thing, the research page's dangling link, fixed on the base branch (e4625dc); main has no Makefile, so `make check` reads 2 there and 0 on the base.
- Nested `claude -p` works inside a Claude Code session (exit 0, measured today), so the builder renders the README itself.
- Run order: `r2-readme` after `packets-7`, cut off `r1-respeak-conventions` merged or not; then packets-5's session row; then the owner re-adds the marketplace and tags v0.6.1.

## Next act
The relay runs row `r2-readme` (builder claude-opus-5-5, 60 turns) off `r1-respeak-conventions`. No NEXT file: the next phase is the relay's.

Exit: landed at 3305db9 plus this charter commit; tree clean; no NEXT; about 16 turns and 80k context of 180k.
