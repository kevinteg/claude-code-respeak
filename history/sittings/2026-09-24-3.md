# Sitting 2026-09-24-3: claude-code-respeak, adv-11-respeak

| row | value |
|---|---|
| goal | adversarial review ADV11 of the code landed since the adv-10 merge (`e661812`); write `design/review-adv11.md`; check every ADV10 high and medium fix as shipped; fix nothing |
| budget | one relay row, 80 turns, 144k context; checkpoint at 64 turns or 80% |
| pin | `main` at `4a3dfa4`; branch `adv-11-respeak` |
| window | 200k, unattended relay run |
| agents | none; the review runs inline |

Doclint's five charter rows, named at open: goal, budget, pin, window, agents.

## Done-ness

- act 0: this charter.
- act 1: `design/review-adv11.md`, six findings, every repro run in a temp directory: 2 high (ADV11-1 nested repo, ADV11-2 git failure; both wrong greens in `respeak-config.py --committed`), 3 medium (ADV11-3 make's last line, ADV11-4 `pgrep` cross-talk, ADV11-5 account pattern), 1 low (ADV11-6, ADV10-8 still open). ADV10-2, 3, 5, 6, 7 hold; ADV10-1 regressed; ADV10-4 half. One `.hygiene-allow` row, `design/review-adv11.md: sibling` (the review names the plugin by its own name; the adv-10 precedent).

Exit: closed 2026-09-24 after act 1; review and charter in one commit; `make check` result in the commit message; nothing pushed.

## Handover

Findings: 2 high, 3 medium, 1 low. Verdict: not ready to gate the device project's docs. The fix that matters most: under `--committed`, `respeak-config.py` counts a project file only when it sits in the target's own work tree, and a git failure exits 2 instead of reading as untracked (ADV11-1, ADV11-2); consumer: the next respeak packet. ADV11-3 and ADV11-5 go to the relay's gate and account stop; ADV11-4 and ADV11-6 to the session plugin's canonical `bounded` suite and doclint. No decision for the owner.

## Next act

None in this row; the next respeak packet cuts the fixes.
