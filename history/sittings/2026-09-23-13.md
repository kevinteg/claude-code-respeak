# Sitting 2026-09-23-13: claude-code-respeak, packets-34-respeak

| item | value |
|---|---|
| goal | write one fix packet, `design/packets/r10-fixes-adv10.md`, from `design/review-adv10.md` and ruling 10 of the run-9 decisions brief, and its rows file `design/packets/rows-2026-09-23-r10.json`; build nothing |
| budget | the CONTEXT line names no `--max-turns`; 60 turns taken, 144k context; checkpoint at 80%; `make check` once, at the end |
| pin | `main` `e661812` (after `adv-10-respeak`); work on `main`, one commit |
| window | 200k, relay-run unattended |
| agents | none; the writer works in-session |

## Done-ness

Done. `design/packets/r10-fixes-adv10.md` (9,206 bytes, doclint clean) and `design/packets/rows-2026-09-23-r10.json` (two rows, 1,557 bytes) written in one commit. Baselines measured on `main` `e661812`: `test_check.sh` 40, `test_render.sh` 31, `test_readme_fresh.sh` 11, `test_gate_hook.sh` 56, `test_bounded.sh` 7 passed; `tests.test_config_layers` OK.

## Handover

- Carried: ADV10-1 and ADV10-2 (the highs), ADV10-3, ADV10-6 (mediums), ADV10-7 (low, falls out of act 2), ruling 10 (final newline), ruling 11 (`make readme` at close). Ruling 10 was not already taken: `README.md` ends without a newline today. Consumer: the builders.
- Deferred: ADV10-4 and ADV10-5, and the `scripts/bounded` half of ADV10-2, go to the session plugin's packet, which owns the canonical `bounded`; they reach this repo by re-sync. ADV10-8 goes to the canonical doclint. ADV6-4 stays its own packet. Consumer: `packets-34-session`.
- Choice: the review's fix for ADV10-1 said an untracked nested project file is exit 3, but its own first test expects BLOCKED. I took the test. An untracked or ignored project file is skipped and named as a drop, and the nearest tracked one governs, read from the index. That keeps a developer's scratch config from reddening every file under it, and it still cannot soften the verdict. Outside git, `--committed` is a setup failure (exit 3). Consumer: the owner, if they want to overrule.
- Choice: an account failure in the renderer exits 4, a code nothing else in the renderer uses. Through `make readme` it reads as 2, so the fixed stderr line `respeak-render: account failure:` is what a sitting reads to stop the run under ruling 1. Consumer: the relay packet writer.
- Choice: the work is split into two rows under the 80-turn cap: r10a builds the two highs, and r10b builds the account failure, the source copy with the newline, and the render. r10b's render keeps `test_readme.py`'s live count green. The inner deadline grace drops to 2 s so it lands before `bounded`'s 5 s. Consumer: the orchestrator.
- No fork is open for the owner.

## Next act

The rows `r10a-fixes-adv10`, then `r10b-fixes-adv10`, driven by the relay; no `history/sessions/NEXT`.

Exit: done, one commit (packet, rows file, charter); make check 0 on the second run (the first read red at hygiene: the packet named a private repo; fixed); tree clean; no NEXT; 2026-09-23.
