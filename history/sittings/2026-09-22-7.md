# Sitting 2026-09-22-7: claude-code-respeak, r4-plugin-and-sync

| row | value |
|---|---|
| goal | packet R4 (`design/packets/r4-plugin-and-sync.md`): doclint re-sync, the `plugin/` subtree, `make doctor`, `make install` if turns remain |
| budget | 80 turns, 60k context; checkpoint at 80% |
| pin | `main` `873ad99`; tags `v0.5.0` to `v0.5.2`, `v0.6.1` untagged so the 0.6.1 entry grows |
| window | 200k, relay-run unattended |
| agents | none |

## Done-ness

1. doclint re-sync and `DOCLINT_SHA`: done, sha256 `b46db3df`.
2. `plugin/` subtree: done; README rendered once, round 1.
3. `make doctor`: pending.
4. `make install`: pending.

## Choices made inside the design

- `examples/layered/README.md` line 6: link target only, to `plugin/docs/config-layers.md`; doclint fails without it (outside the packet's edit list).
- `plugin/docs/config-layers.md` links `../../examples/layered/`: a citation of the repository tree, dead in an installed cache as it was before.
- `docs/architecture.md` diagram line 29 keeps `corpus/lexicon.yaml`: the box has no room for `plugin/`; the prose below names the full path.
- `.gitignore` comment still cites `docs/config-layers.md`: not in the edit list.

## Handover

## Next act

Act 3, `make doctor`.

## Exit

(open)
