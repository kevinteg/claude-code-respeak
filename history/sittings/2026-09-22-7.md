# Sitting 2026-09-22-7: relay row r4-plugin-and-sync

- Packet: `design/packets/r4-plugin-and-sync.md` (R4). Branch `r4-plugin-and-sync` off `main` `873ad99`.
- Budget: 80 turns, 60k context; commit per act; never push, amend or rebase.
- Open state: tags `v0.5.0` to `v0.5.2`; `v0.6.1` untagged, so the 0.6.1 CHANGELOG entry grows.

## Acts

1. doclint re-sync (conventions ceiling) and `DOCLINT_SHA`: pending.
2. `plugin/` subtree: pending.
3. `make doctor`: pending.
4. `make install`: pending (only if turns remain).

## Choices made inside the design

## Handover

## Exit

(open)
