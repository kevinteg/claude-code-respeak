# Sitting 2026-09-23-10: claude-code-respeak, packets-29-respeak

| item | value |
|---|---|
| goal | write packet R9 (`design/packets/r9-readme-r7.md`) and its rows file (`design/packets/rows-2026-09-23-r6b.json`): the public-facing README source, the status line the renderer keeps honest, the icon at the shared path, the public-repo check; build nothing |
| budget | this table is doclint's five rows; 40 turns, 144k context; one commit; `make check` once; checkpoint at 80% |
| pin | `main` `2c8ec97` (r8 merged); the session's conventions §5 and §6 and the site-common icon generator read once and quoted into the packet |
| window | 200k, relay-run unattended |
| agents | none; the coordinator measures and writes in-session |

## Done-ness

Done. Measured on `main` `2c8ec97` under the virtualenv (3.12.7): 219 unittest cases, 10 bash suites, `tests/test_render.sh` 26 passed, `readme: fresh`, the sidecar checks; the source 21,758 bytes with a 5,335-byte References section and a FAIL on `max_sentence_words` (46, budget 35); the public-group scan over the README and the source reads 0, the only sibling id is `claude-code-session`. The packet, the rows file and this charter are one commit; `make check` ran once, green, before it.

## Handover

- The rows file's name: the prompt named `rows-2026-09-23-r6.json`, which already holds the merged `r8-resync` row (c8d1478, merged at 2c8ec97), a record; the new row went to `rows-2026-09-23-r6b.json`, the `r4b` precedent. Consumer: the relay orchestrator, which reads row `r9-readme-r7` from the b file.
- The icon: the megaphone stays (the owner named respeak's icon as the family); the file moves to `assets/icon.svg`, the path the four plugins share; no PNG (the family's generator writes SVG masters only). Consumer: the builder; the sibling packet writers, whose icons take distinct glyphs.
- References move whole to `docs/references.md` to make room under the 24,576-byte source ceiling for install, commands, tests and conventions; the README links it. Consumer: the owner, to overrule (the ceiling could rise instead).
- The status line's count is the unittest collection plus the bash suite count, not a run (a run is minutes under the deadline; a collection is 0.1 s). Consumer: the builder.
- The public-repo check: the hygiene scan for every group but `person` and `sibling` over the README and the source reads 0, no `../` sibling path, no sibling id but the provider's. `sibling` cannot read green on a README that names the plugin; the allow rows stay as they are. Consumer: the builder; the orchestrator, whose prompt said sibling green.
- Render count here: none; the builder's is at most two, counted in its report. Consumer: the orchestrator.
- Open fork for the owner: none.
- Exit: the packet, `rows-2026-09-23-r6b.json` and this charter in one commit, `make check` 0, tree clean, no `history/sessions/NEXT`, 2026-09-23.

## Next act

None here; the relay runs row `r9-readme-r7` off `main` from `design/packets/rows-2026-09-23-r6b.json`.
