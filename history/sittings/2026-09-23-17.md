# Sitting 2026-09-23-17: claude-code-respeak, r11-resync

| item | value |
|---|---|
| goal | build packet R11 (`design/packets/r11-resync.md`): re-sync hygiene, doclint and bounded to the session's `330bded`, then close the `bounded` half of ADV10 with tests; one commit per act |
| budget | 50 turns, 144k context; checkpoint at 80% |
| pin | `main` `c5418cf` (packet commit); branch `r11-resync`; the session's `main` `330bded` read as three blobs |
| window | 200k, relay-run unattended |
| agents | none; the builder works in-session |

## Done-ness

Done. Act 1 is committed (`4045026`): the three scripts are at `efc59152 b924a7bc 63cebd3d`, the pins have moved, the wall is 840, and every packet carries a `verify` row. Act 2 is committed with this charter: nine new cases in `tests/test_bounded.sh` (18 passed), the `Makefile` comment on what `make check`'s exit means, and one CHANGELOG bullet. `make check` read 0 at both acts (2 of 2 allowed). The one run against the old copy (`git show HEAD~1:scripts/bounded`, `e6079e68`) gave 12 passed, 6 failed: the three stop lines (it printed `bounded: wall of 1 s reached; ...`, `... held by pid N for 0 s (stale at 900 s)` and `bounded: load 1000.00 above 64.00 ...`), both lines of the aged-holder case, and one `sleep 353` survivor, which the trap killed.

## Handover

- Choice (act 1): the packet measured r6 at 9,215 bytes with the row added; it came to 9,220, 4 bytes over the 9,216 ceiling. r6's `acceptance` cell drops its closing clause "the gate reruns them on the branch", as §2 rules for r7 and r10, because the new `verify` row states it (r6 is now 9,184 bytes). Consumer: the orchestrator.
- Choice (act 2): §2's two ADV10-5 cases (the two-runner race and the dead-owner directory) pass on the old copy too, because mkdir was already atomic and the old copy already took over a dead owner's lock. So they are not red against the old copy. One added case is: a live holder whose lock is 1000 s old still refuses with 2. The old copy took it over past 900 s. `BOUNDED=<path>` lets the suite run against another copy. Consumer: the orchestrator.
- Choice: the charter is `2026-09-23-17`, from the clock date (the rows file names 2026-09-24). Consumer: the orchestrator.
- Left undone, per §2: `design/readme/source.md` still says "a 900 s wall" (the next README round); the gate reading the stop line (agent-relay's re-sync row). No open fork.

## Next act

The orchestrator merges `r11-resync`; no `history/sessions/NEXT`.

Exit: done, two acts committed, make check 0 at each, tree clean, 2026-09-23.
